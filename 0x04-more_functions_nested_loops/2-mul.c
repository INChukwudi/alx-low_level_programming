#include "main.h"

/**
 * mul - the function multiplies two integers passed in
 * @a: the first number
 * @b: the second number
 *
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
